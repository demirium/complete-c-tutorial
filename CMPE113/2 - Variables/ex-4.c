#include <stdio.h>

int main(void) {
    char grade = 'C';
    float gpa = 3.45;

    printf("Grade: %c, GPA: %.2f\n", grade, gpa); // We can write more than one variable in printf function

    return 0;
}